import {GraphChange} from './graph-change';

describe(
    'GraphChange',
    () => {
        // it('should create an instance', () => {
        //  expect(new GraphChange()).toBeTruthy();
        //});
    });
